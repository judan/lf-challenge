import styled from 'styled-components';
import t from '../../styles/tokens';
import { base } from '../../styles/utilities/base';

const ArtworkGridStyled = styled.div`
  ${base}
`;

export default ArtworkGridStyled;